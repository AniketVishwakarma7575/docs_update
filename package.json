{
  "name": "node-crud-app",
  "version": "1.0.0",
  "private": true,
  "description": "A simple dependency-free Node.js CRUD application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/run-tests.js",
    "docs:ai-sync": "node scripts/ai-docs-sync.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "multer": "^2.2.0"
  }
}
